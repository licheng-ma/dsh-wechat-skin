# dsh-wechat-skin profile patch layer — insert one loader entry for the plugin
# package, mirroring dsh-dream-skin. The browser half is picked up by
# dsh-client-modules through the package's dsh.client declaration.
- insert:
    - id: wechat-skin
      name: 'dsh-wechat-skin'
